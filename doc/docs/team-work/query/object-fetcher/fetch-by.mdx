---
sidebar_position: 6
title: 暴露给客户端API
---

得益于Jimmer不可变对象的设计，对象抓取器也是不可变的，这带来了一系列好处。

比如可以将对象抓取器放置在任意位置而不用担心多线程安全问题。

因此我们可以在controller层书写对象抓取器，然后将参数或返回值用@FetchBy进行修饰，在通过配置文件开启前端zip包下载后

解压即可得到后端请求参数与返回参数，可以减少大量核对文档和手动书写重复请求参数和返回值的时间。

具体示例请查看[Spring篇/前后端免对接](../../spring/client)