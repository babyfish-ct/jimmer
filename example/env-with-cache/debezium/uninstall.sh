docker stop debezium-demo-redis
docker rm debezium-demo-redis

docker stop debezium-demo-connect
docker rm debezium-demo-connect

docker stop debezium-demo-postgres
docker rm debezium-demo-postgres

docker stop debezium-demo-kafdrop
docker rm debezium-demo-kafdrop

docker stop debezium-demo-kafka
docker rm debezium-demo-kafka

docker stop debezium-demo-zookeeper
docker rm debezium-demo-zookeeper

