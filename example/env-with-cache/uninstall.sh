docker stop jimmer-demo-maxwell
docker rm jimmer-demo-maxwell
docker stop jimmer-demo-kafka
docker rm jimmer-demo-kafka
docker stop jimmer-demo-zookeeper
docker rm jimmer-demo-zookeeper
docker stop jimmer-demo-mysql
docker rm jimmer-demo-mysql
docker network rm jimmer-demo-network
