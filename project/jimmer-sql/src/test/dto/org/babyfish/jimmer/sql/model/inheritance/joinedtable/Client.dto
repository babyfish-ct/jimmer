import java.lang.Deprecated
import testpkg.Named

@Deprecated
ClientExhaustiveView {
    id
    name
    #subtypes {
        #exhaustive
        @Deprecated
        Organization class OrganizationBranch implements Named {
            taxCode
            id(projects) as projectIds
        }
        Person {
            firstName
            lastName
        }
    }
}
