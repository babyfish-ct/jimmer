SimpleBook {
    id
    name
}

ComplexBook {
    #allScalars
    store {
        #allScalars
    }
    authors {
        #allScalars
    }
}

input BookInput {
    #allScalars
    id(store)
    id(authors) as authorIds
}
